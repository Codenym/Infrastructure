terraform {
  cloud {
    organization = "codenym"
    workspaces {
      name = "codenym-dagster"
    }
  }

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.16"
    }
  }

  required_version = ">= 1.2.0"
}

provider "aws" {
  region = "us-east-1"
}
